var substring = function (val, start, end) {
    if (val.length == 0 || val == undefined) {
        return;
    }
    return val.substring(start, end);
}
module.exports = {
    substring: substring
};